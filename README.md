# RAG_over_Constitution_of_India
A RAG-based question answering system built in Python that loads PDF documents, splits them into chunks, generates embeddings using SentenceTransformers, stores them in ChromaDB vector database, and retrieves relevant context to answer queries using Google Gemini AI.
